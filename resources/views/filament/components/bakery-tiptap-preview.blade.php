<div dir="rtl" class="tiptap-content prose prose-sm max-w-none dark:prose-invert sm:prose-base">
    @if ($html !== '')
        {!! $html !!}
    @else
        <p class="text-sm text-gray-500 dark:text-gray-400">هنوز محتوایی برای پیش‌نمایش وارد نشده است.</p>
    @endif
</div>
